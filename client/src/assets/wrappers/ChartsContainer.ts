import styled from 'styled-components';

const Wrapper = styled.div`
  margin-top: 4rem;
  gap: 1rem;
  display: grid;
  place-items: center;
  button {
    font-size: 1rem;
  }
`;

export default Wrapper;
