import { IMonthlyProducts } from '../types/Products';

const AreaChart = ({ data }: { data: IMonthlyProducts[] }) => {
  console.log(data);
  return <div>AreaChart</div>;
};

export default AreaChart;
